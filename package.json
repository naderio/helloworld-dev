{
  "private": true,
  "name": "helloworld-dev",
  "version": "0.1.0",
  "scripts": {
    "format": "prettier --list-different --write '.*.{yml,json}' '*.{md,yml,json}' 'docs/**/*.{md,yml,json}' 'services/*/*.{md,yml,json}'",
    "services:cleanup": "sudo rm -fr ./services/*/data.tmp/"
  },
  "devDependencies": {
    "prettier": "~2.2.1"
  }
}
