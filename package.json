{
  "private": true,
  "name": "helloworld-dev",
  "version": "0.1.0",
  "scripts": {
    "format": "prettier --cache --list-different --write '.*.{yml,json}' '*.{md,yml,json}' 'docs/**/*.{md,yml,json}' 'services/*/*.{md,yml,json}'",
    "cleanup": "sudo rm -fr ./data.tmp"
  },
  "devDependencies": {
    "prettier": "~2.7.1"
  }
}
